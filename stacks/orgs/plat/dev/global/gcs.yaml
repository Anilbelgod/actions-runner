terraform:
  backend_type: gcs
  backend:
    gcs:
      bucket: cch-state
components:
  terraform:
     google-storage-bucket:
        vars: 
          namespace: cch
          tenant: plat
          stage: dev
          environment: gbl